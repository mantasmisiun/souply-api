node_modules/
dist/
.env
.env.test
schema.sql
# ...but the structure-only test schema MUST be tracked — CI loads it to
# bootstrap the empty souply_test_ci database (no data, no secrets).
!tests/schema/schema.sql

# Transient debug outputs
swipe-debug.log
mismatches.csv
*.log
*.csv
# Generated receipt batch outputs
receipts/_batch_staging/
receipts/_logs/
# Recipe-import validation corpus: harvested HTML and sweep output are
# disposable and huge — regenerate them with `npm run recipes:harvest`.
# receipts/_recipes/urls/ stays TRACKED: the URL lists are the expensive part.
receipts/_recipes/pages*/
receipts/_recipes/tables/
# Raw receipt images (PII)
receipts/lidl/
receipts/maxima/
receipts/norfa/
receipts/rimi/
receipts/iki/
.env*
!.env.example
!.env.production.example
receipts/_recipes/slices_*/
receipts/_recipes/verdicts_*/

# Raw receipt fixtures (PII) — untracked 2026-08-13
receipts/receipt pdf/
receipts/receipt images/
receipts/base64/
receipts/parsed json/
receipts/request json/
receipts/*.xlsx
.env.bak*

# Operational runbooks — kept out of the public repo
INFRA.md
scripts/BACKUP.md
